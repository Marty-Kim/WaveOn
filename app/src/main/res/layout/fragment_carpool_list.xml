<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_primary">

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_carpool_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabIndicatorColor="@color/ocean_blue"
        app:tabSelectedTextColor="@color/ocean_blue"
        app:tabTextColor="@color/gray_medium"
        app:tabIndicatorHeight="4dp"
        android:background="@color/background_surface"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_carpool"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="12dp"
        android:clipToPadding="false"
        android:overScrollMode="never"
        tools:listitem="@layout/item_carpool_card" />

</LinearLayout> 